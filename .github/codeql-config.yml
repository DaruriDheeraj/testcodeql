name: "Custom CodeQL Config"
disable-default-queries: false
queries:
  - uses: security-and-quality
  - uses: codeql/javascript-queries:hardcoded-credentials
  - uses: codeql/python-queries:hardcoded-credentials
